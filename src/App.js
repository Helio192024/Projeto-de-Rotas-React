
import AppRoutes from "./routes";


function App() {

    return (
        <AppRoutes />
    );
}

export default App;





{/* /* { categories.map(( category, index ) =>
 <Category Category={category}>
 {filterCategory(index).map((video) => <Card id={video.id} key={id} /> ))}
 </Category> */}
